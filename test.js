let surrounding = {
    // no spaces in key
    "[-1,0]" : [[1, 2], [2, 1], [0, -2]],
    "all": [[1, 2]]
};
// let direction = [-1, 0];
// surList = surrounding[JSON.stringify(direction)];
let v = "all"
console.log(surrounding[v])

